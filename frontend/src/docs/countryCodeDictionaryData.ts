/**
 * Re-export for deployments that still import from `docs/`.
 * Canonical data lives in `src/lib/countryCodeDictionaryData.ts`.
 */
export {
  COUNTRY_CODE_DICTIONARY,
  type CountryCodeDictionaryRow,
} from '../lib/countryCodeDictionaryData';
